---
name: live-action-drama-producer
description: "Orchestrate a live-action AI short-drama run from a local script and asset folder: split 30-second V4.2 shots into balanced 70–90-word source ranges, write example-style detailed director prompts, preserve their exact text in 剧梦, bind assets with 一键引用, use narration only as silent visual anchors, preserve continuity with actual tail frames and blocking composites, generate and QC videos, synthesize/timestamp/mix narration later, and recommend/download BGM without mixing it. Use for 全流程生成、剧梦生视频、V4.2均衡拆镜、旁白无声画面锚点、连续分镜防穿帮、尾帧续接、剪辑缓冲、站位合成图、缺失资产补全、资产引用自检、短剧旁白回填或短剧配乐任务."
metadata:
  version: 1.7.0
  language: zh-CN
---

# Live-Action-Drama-Producer v1.7.0

把用户提供的剧本和资产目录转化为可交付的原始分镜视频、纯旁白音频、旁白版分镜视频与独立 BGM 候选。通过浏览器控制实际平台；V4.2 对英文等空格分词剧本按每个完整 30 秒 70～90 个原文单词均衡拆镜，最后一镜不足 30 秒时按实际容量设计；使用用户确认的示例式导演稿结构生成详细提示词，并把确认稿原文一次不差地放入剧梦；旁白可固定画面但绝不进入原视频音轨；缺失的必要视觉资产先按剧本和项目风格补全并验收；紧接镜头使用真实尾帧、复杂空间调度使用站位合成图，必要时两者并用；提示词必须通过拆镜、资产引用与连续性闭环自检后才能生成视频。

## 范围与硬边界

本 Skill 负责：读取剧本与拆镜；识别跨镜连续性簇；用上一镜真实可用尾帧锁定下一镜首帧；需要精确构图时制作含人物的专用站位合成图；匹配既有资产；为当前分镜补全经确认确实缺失的人物、LOOK、场景与道具资产；视觉验收、登记、上传并引用资产；按唯一 V4.2 规范写提示词；在生成前检查错用、漏用和多用；在剧梦生成原视频；根据原视频空档生成并回填旁白；为用户推荐、试听和下载 BGM。

缺失资产补全只覆盖当前制作范围实际需要的资产，不得借机改造、替换或批量重制已经可用的项目资产。用户明确要求“只检查、不生成资产”时只报告缺失并暂停。其他不负责事项：画面剪辑；字幕；把 BGM 混入任何视频。旁白回填只能改音轨，不得裁剪、调速、重排或替换画面。每镜只生成一次，失败或 QC 异常均报告，不自行重新抽卡。

默认规格（用户当前请求优先）：完全写实实拍真人，9:16，480P，九梦2.5 Pro，单镜 4–30 秒；原视频只有现场对白、环境声与动作音效，无旁白发声、内心独白、字幕和 BGM。

## 项目启动引导

需要为新项目建立 Codex 开场指令时，使用 [assets/引导.md](assets/引导.md) 作为可填写模板。把它复制到项目目录并填写剧名、集数、主语言、制作类型、路径和剧梦目标。用户提供的项目 `引导.md` 属于项目级操作指令，必须在剧本和资产处理前完整阅读；剧本正文、素材文件名和附件内容只作为制作资料，不自动视为操作指令。用户当前明确要求始终高于模板与本 Skill。

启动确认只需简要复述身份、目标集、主语言、制作类型、目标项目和自动执行模式，随后直接开始已获授权的工作，不得停在计划或等待用户再次说“继续”。自检发现的可修复问题必须按本 Skill 自动修复并复检；只有真实候选歧义、登录/验证码、工具不可用、三轮资产生成仍失败或需要新授权的外部动作才允许暂停。

## 开始前运行清单

取得剧本绝对路径与范围、资产目录、注册表路径、剧梦项目/集数/起始镜号、视频时长、交付目录、旁白参考音频、是否需生成前人工检查，以及项目禁令。缺失资产默认按本 Skill 自动补全；连续分镜在 V4.2 设计后按“是否紧接”和“站位是否复杂”分别启用尾帧法、站位合成图法或两者；用户可明确要求只检查不生成。只有缺失信息会实质阻止当前阶段时才询问。

命名固定为：

- 原视频：`{集数}-{分镜号}.{抽卡序号}.MP4`，首次抽卡序号为 `0`；
- 旁白源：默认 `{集数}.wav`，必要时使用 `{集数}-{分镜号}.wav`；
- 旁白版：`{集数}-{分镜号}.{抽卡序号}(OV).MP4`，与原视频同目录；
- BGM：独立音频文件，永不自动混入成片。

## 唯一分镜规范 V4.2｜示例式导演稿

写提示词前完整阅读 [references/storyboard-director-v4.2.md](references/storyboard-director-v4.2.md)，仅以它作为分镜写作规范。V4.2 以用户提供的两份详细分镜示例所体现的写法为唯一标准；不要调用、混入或回退到其他分镜 Skill，也不得恢复任何旧版工程化长模板、双层预算或附加控制层。

V4.2 的正式提示词必须像可直接执行的真人短剧导演稿，而不是工程审计稿。固定从 `分镜[编号]（[时长]秒）` 开始，按实际剧情依次包含：`场景`、`本分镜固定锁定`、`人物初始状态` 或 `人物初始站位与朝向`、按时间排列的 `0–X秒｜机位A` 等机位段、每段完整的摄影与表演说明、现场对白或无声旁白画面锚点、人物动作/表演链、空间与连续性锁定、`最后一帧固定` 或等价结束状态、`音效`。20～30 秒分镜通常使用 5～8 个有明确叙事职责的小机位；每个机位都要详细写清焦段、机位高度与方位、前中后景、物理光源、焦点变化、叙事目的、动作起因与余势、身体重心、视线、面部、呼吸、说话者及非说话者反应。不得只列栏目或压缩成摘要。

V4.2 不使用旧版固定字符配额，也不以重复通用禁令凑篇幅。长度由当前剧情和镜头复杂度决定；以“每个小机位已经达到示例级执行颗粒度、整镜剧情能在时长内自然完成”为通过标准。正式稿仍不得超过剧梦 15000 字符上限。信息无法在 30 秒内自然完成时继续拆镜，不加速对白、不牺牲动作过程。

旁白防误读必须在 V4.2 正文创作阶段自然写入相应位置：旁白原文只写成 `旁白画面锚点｜不开口，不生成旁白声音` 或等价明确表述，并在 `音效` 中锁定只允许现场对白、环境声和动作音效。不得在正文完成后再添加音频前缀、黑名单块或任何平台控制段。`scripts/prompt_voiceover_guard.py` 只允许使用 `--check-only` 做只读检查，禁止用它生成或改写平台版提示词。

完成正式稿后，把它保存为唯一批准源文件并运行 [scripts/v4_prompt_completeness_guard.py](scripts/v4_prompt_completeness_guard.py)。只有取得 `V4_2_TEMPLATE_PREFLIGHT_PASS` 才能进入剧梦录入。上传前后必须逐字符核对正文；平台自动规范化的单个文件末尾换行可以忽略，除此之外不得删除、摘要、改写、重排、追加或插入任何字符。不得添加 `V4_ADDON`、资产白名单、工作流说明、连续性操作说明、自检结果、状态码、token 占位符或完整资产文件名。

剧梦资产绑定必须在正文逐字符录入并复核后进行：使用页面的 `一键引用`，把正文中已经存在的自然实体名称映射到已确认资产。不得为了引用而在正文末尾追加资产清单、占位符或 Skill 指令。映射名称必须是正文中的真实人物、场景或道具名称，并避免使用会误伤其他词语的过短子串。确认后，平台把匹配名称渲染为富文本 token 属于资产绑定显示，不视为改写导演内容；同一正确资产因多次自然出现而被一键引用多次是允许的。引用后不得再次整段粘贴或全量替换正文，否则 token 作废并必须从头重做一键引用。

最终送入生成模型的内容只能是：**已确认的 V4.2 导演稿正文 + 剧梦一键引用形成的富文本资产 token**。其他 Skill 功能只指导代理如何拆镜、补资产、处理连续性、自检和操作平台，绝对不能被粘贴进视频生成提示词。

## 资产注册、匹配与上传门禁

涉及资产时完整阅读 [references/asset-naming.md](references/asset-naming.md) 和 [references/asset-registry-field-guide.md](references/asset-registry-field-guide.md)，以 [assets/asset-registry-template.csv](assets/asset-registry-template.csv) 建表。

流程：

1. 枚举资产目录并用 [scripts/asset_registry_audit.py](scripts/asset_registry_audit.py) 计算/核对 `file_size`、`modified_time`、`sha256`；
2. 将剧本实体、LOOK、场景、道具映射到唯一注册表记录；
3. 视觉检查候选图是否符合身份、服装、场景状态与连续性；
4. 仅当匹配为 `HIGH`、一对一且不存在竞争候选时，允许自动标记 `READY`；
5. 平台尚无该资产时，上传本地文件，核对名称和缩略图，记录 `platform_asset_id`；
6. 若存在多个候选、名称冲突、LOOK/状态不清、视觉内容不符或无法判断，标记 `PENDING_CONFIRMATION`，明确列出候选与原因并询问用户；用户回答前暂停该镜，绝不能猜；候选歧义不等于资产缺失，禁止绕过歧义另造资产；
7. 文件指纹发生变化时清空旧验证结果，回到待检查状态；
8. 只允许 `READY` 资产进入提示词绑定和页面一键引用。

不再要求用户逐项确认所有明确资产；用户确认只用于真正歧义。完成全目录、别名、注册表、平台名称和缩略图搜索后仍不存在的必要资产，才是“确认缺失”。确认缺失时完整阅读 [references/missing-asset-generation-loop.md](references/missing-asset-generation-loop.md)，生成、验收、保存、登记和上传后才能置为 `READY`；生成失败或三轮针对性修正后仍未通过时停止受影响分镜，绝不能跳过引用。

## 端到端流程

### 1. 读取、分类与拆镜

完整读取指定范围，区分现场对白、旁白/心理叙述、动作和场景说明。输出内部边界表：集数、分镜号、原文起止、`coverage_words`、预计时长、与完整镜平均词数的偏差、现场对白、旁白、所需资产、下一镜禁入剧情。

对英文及其他以空格分词的剧本，每个完整 30 秒分镜必须连续覆盖 70～90 个原剧本单词。只统计实际承接的原始对白、原始旁白/心理叙述和有意义动作/叙事文字；不统计集数、场次标题、人物标签、时间码、资产名、技术说明和新增导演文字。标点不计词，带撇号词和原文中的连字符复合词各按一个词。非空格分词语言使用项目已确认的分词器或用户指定等价口径，不能临时换口径凑数。

较长剧集除最后一镜外全部使用 30 秒，并把各完整镜尽量均衡到约 80 词；所有完整 30 秒镜必须在 70～90 词内，正常情况下最大词数差不得超过 10。最后一镜若自然支撑 30 秒，同样必须为 70～90 词；若按正常对白、动作和停顿确实达不到 30 秒，则按实际容量设计 4～29 秒，不填充、不重复、不拖慢，且不得超过 90 词。若最后一镜不足 4 秒，回调前镜边界重新拆分。所有镜必须从原文第一词到最后一词连续覆盖，无遗漏、重叠、倒序或改写。

把各镜实际原文填入 [scripts/storyboard_word_budget.py](scripts/storyboard_word_budget.py) 的 JSON 输入并运行校验。只有词数、时长、均衡度和分镜次序全部通过后才记录 `SCRIPT_SPLIT_PREFLIGHT_PASS`；失败时重新划分。无法形成合法边界时标记 `SCRIPT_SPLIT_CONSTRAINT_BLOCKED` 并停止生成受影响提示词，绝不能静默超出范围。

先按 V4.2 的剧情边界和导演逻辑完成拆镜，再标记 `continuity_link`：同一场景、同一时间、同一段未演完的动作、对白或故事被时长迫使拆开的相邻分镜标为 `HARD`；同一地点但已切到下一段行动或存在明显时间跳转标为 `SOFT`；切到下一天、其他场景、其他时空或无直接连续关系标为 `NONE`。同一个场景本身不自动等于 `HARD`。`HARD` 分镜组成连续性簇，必须顺序生成，不能在上一镜真实视频和连续性移交尚未完成时先生成下一镜。完整规则见 [references/cross-shot-continuity.md](references/cross-shot-continuity.md)。

拆镜时必须先做音频容量预检：`预计对白秒数 + 预计旁白画面锚点所需时间（按后续安全语速不高于 1.25 估算）+ 每个对白/旁白交界至少 0.35 秒保护量 + FRAME/BOTH 镜头的 1–2 秒尾帧识别缓冲 <= 该镜时长`。不使用尾帧时缓冲项为零。不满足时在生成提示词前继续拆镜；不得假设生成后可以靠压低对白、强行叠音、占用剪辑缓冲或超过安全语速解决。FRAME/BOTH 的完整 30 秒镜仍须覆盖 70～90 词，但这些内容必须能在扣除缓冲后的 28～29 秒内自然完成。平台提示词必须为每段旁白安排语义对应的“无人物对白、无人开口、无人对口型”的画面窗口；旁白原文只固定画面，不触发任何声音。

### 2. 生成并校验资产绑定

为每镜建立 `required_asset_keys`。人物本体与 LOOK 分列，每个实际出现的独立空间至少一个场景，关键道具按实际状态列出。核对本地文件、注册表、平台资产及页面 token 四向一致。高置信唯一项可自动上传并继续；任何歧义按上述门禁中断；确认缺失项按缺失资产闭环补全，不得静默删去需求。

### 3. 生成平台提示词

仅在 `SCRIPT_SPLIT_PREFLIGHT_PASS` 后按唯一 V4.2 规范写示例式导演稿。运行完整度门禁并取得 `V4_2_TEMPLATE_PREFLIGHT_PASS`；再用 `prompt_voiceover_guard.py --check-only` 做只读旁白检查，不得生成“平台版”、不得通过适配器改写正文。检查剧情不漏、不重、不提前；位置、轴线、服装、道具连续；对白与旁白分类正确；旁白只作为无声画面锚点；所有实际可见资产均进入绑定表；没有多余资产。通过后把该正文保存为唯一批准源，之后不得改动任何字符。

把批准的 V4.2 正文逐字符填入剧梦，再使用页面 `一键引用` 完成资产映射；不得在正文末尾追加资产清单、`@名称`、token 占位符、连续性说明、自检结果或任何 Skill 操作文字。随后完整阅读并执行 [references/v4-asset-reference-preflight.md](references/v4-asset-reference-preflight.md)。资产引用数量没有上限；不得为减少 token 数量而省略必要资产。普通文字资产名或素材栏图片都不算绑定，只有一键引用后形成的平台富文本 token 才算引用。

若自检发现确认缺失资产，转入 [references/missing-asset-generation-loop.md](references/missing-asset-generation-loop.md)；补全、上传和形成 token 后重新从头执行引用自检。只有页面唯一 token 集合与 `required_asset_keys` 完全一致，且错用、漏用、多用和未确认歧义均为零时，状态才是 `ASSET_PREFLIGHT_PASS`。若用户要求人工检查，在此状态后、生成按钮前暂停；否则继续。

### 4. 紧密连续分镜防穿帮

V4.2 先完成剧情拆镜和下一镜导演设计，再完整阅读并执行 [references/cross-shot-continuity.md](references/cross-shot-continuity.md)。分别做两个独立判断：

1. 下一镜是否为同一场景、同一时间、同一段尚未演完的故事、动作或对白？若是，标记 `HARD` 并使用 `FRAME` 尾帧法；若已切到下一天、其他场景、其他时空或新段落，则不使用尾帧控制。
2. 下一镜是否存在多人、复杂站位、精确遮挡、前中后景、换机位后的空间关系或关键道具位置需要预设？若是，使用 `STAGING` 站位合成图法，不论是否使用尾帧。

据此记录 `continuity_control=NONE | FRAME | STAGING | BOTH`：非紧接且构图简单为 `NONE`；紧接且简单为 `FRAME`；非紧接但站位复杂为 `STAGING`；紧接且站位复杂为 `BOTH`。尾帧法和站位合成图法都用于辅助 V4.2，而不是替代 V4.2；两种方法也不是互相兜底。

`FRAME` 锁上一镜真实结束状态和动作余势；`STAGING` 锁下一镜计划中的开场构图、站位、朝向、遮挡与相对尺度；V4.2 仍负责剧情边界、动作过程、表演、摄影机运动和声音。选用 `FRAME` 时从上一镜真实视频末段提取并视觉选择可用帧，并在下一镜开头安排 1–2 秒尾帧识别缓冲：保持与上一镜结尾相同的机位、构图、站位、人物/道具状态和光线，不进入新剧情、不开新对白、不立即运镜；缓冲后立刻明确 CUT 到 V4.2 设计的新机位或正式动作。该段计入生成时长，但不计入有效剧情容量，作为模型稳定识别和后期裁切使用的剪辑手柄。

选用 `STAGING` 时按 V4.2 构图用空场景、人物/LOOK、关键道具和必要的实际尾帧制作。`FRAME` 与 `STAGING` 均不能代替 `CHAR`、`LOOK`、`SCENE` 或必要 `PROP`；即使尾帧已经完整显示人物与场景，也必须正常引用本镜全部基础资产。

任一视觉辅助与 V4.2 或基础资产冲突时标记 `CONTINUITY_PREFLIGHT_BLOCKED`，先修订 V4.2 和/或站位图并重新自检。只有所选控制、连续性移交、页面 token 和 V4.2 全部对齐后才允许继续。

### 5. 剧梦生成原视频

按 [references/platforms.md](references/platforms.md) 操作。没有可验证的 `SCRIPT_SPLIT_PREFLIGHT_PASS` 和 `ASSET_PREFLIGHT_PASS` 时绝对不能点击生成。通过后再次核对模型、比例、画质、时长及页面 token 集合，只点击一次生成，完成后播放验证并按固定规则下载。使用 `FRAME` 或 `BOTH` 时，总时长必须额外容纳 1–2 秒尾帧识别缓冲；不能通过挤压对白、旁白画面锚点或后续剧情来腾出该段。

`HARD` 连续性簇按“当前镜生成与 QC → 提取可用尾帧 → 准备下一镜 → 下一镜自检与生成”循环执行，禁止整簇同时排队抽卡。

### 6. 原视频 QC

完整执行 [references/quality-control.md](references/quality-control.md)：先运行 [scripts/media_qc.py](scripts/media_qc.py) 做解码、元数据、黑帧、冻结和音轨检查，再人工/视觉检查身份、LOOK、场景、连续性、穿帮，以及原视频是否误生旁白、字幕或 BGM。异常记录时间码与证据，状态设为 `REVIEW_REQUIRED` 或 `FAIL`；未经用户指令不重生成。

### 7. 依据真实视频生成旁白

先分析已生成视频中的现场对白区间和可用无对白窗口，再确定旁白进入点、退出点与可用秒数。完整阅读 [references/narration-mix.md](references/narration-mix.md)，用 [scripts/narration_speed.py](scripts/narration_speed.py) 估算语速：默认 1.1，允许范围 0.95–1.25。

对白时间轴必须来自下载后的真实视频，并经带时间码的语音识别/人声活动检测与逐段播放复核；提示词预计时间、声波图或静音检测不能单独作为依据。每个对白区间前后默认加入 0.35 秒保护区。无法可靠取得或复核对白时间轴时暂停旁白回填，不得猜测。

- 默认 1.1 能放下时保持 1.1；
- 需要提速时仅提高到刚好放下的值，最高 1.25；
- 超过 1.25 仍放不下时中断并报告，不叠对白、不删文本、不改变视频速度；
- 把所有受保护对白区间从视频时长中剔除后，必须按剧情顺序把旁白完整放入剩余窗口；任一段无处可放即中断；
- 同一集各镜可共用一个 `{集数}.wav`，但必须用 cue sheet 精确切段；若不同分镜需要的语速无法由同一条集级音频兼容，改为逐镜生成 `{集数}-{分镜号}.wav`。

Voice Clone 参数按平台参考执行。生成后试听并测量实际 WAV；估算通过但实测超出窗口时，最多在安全范围内按新计算值重新生成该旁白音频，不能重新生成视频。

### 8. 固定方式回填旁白

建立 JSON cue sheet，记录源 WAV 起止、目标视频起止、原文，以及经真实视频复核的 `dialogue_timeline`。必须调用 [scripts/mix_narration.py](scripts/mix_narration.py)，禁止临时拼接未经审计的 FFmpeg 命令。脚本必须在写文件前校验每个旁白 cue 与全部对白保护区零交叉；缺少真实时间轴声明、时间轴无效或存在任何交叉时直接失败，不得输出 `(OV)` 文件。

工具复制视频流、只重编码音频，并保留原始对白与音效。`duck_gain` 只用于旁白窗口中的环境声和动作声，绝不能作为遮盖对白冲突的手段。输出 `(OV).MP4` 后再次运行媒体 QC，并逐段试听对白交界、截字、重叠、削波和时长；只要发现对白与旁白同时说话，结果立即标记 `FAIL`。

### 9. 推荐与下载 BGM

按平台参考读取剧情弧线与旁白版成片，至少试听主推荐和一个对照候选，优先器乐/无人声。记录曲名、平台 ID、类型、理由和本地路径。只下载独立 BGM 文件，绝不自动混入原视频或 `(OV)` 视频；用户自行试配。

## 浏览器执行原则

- 使用可用的 Computer Use/浏览器能力，遵循系统即时确认要求；
- 依靠可见文本和页面状态，不盲点固定坐标；
- 上传后核对文件名、缩略图和平台 ID，设置控件后读取当前值；
- 长时间生成只等待明确完成状态，不重复点击；
- 登录失效、验证码、平台错误、资产歧义或目标项目不明确时停在安全位置并报告。

## 完成标准

交付记录列出：分镜边界、每镜 `coverage_words`、时长、均衡度与 `SCRIPT_SPLIT_PREFLIGHT` 状态；连续性簇和状态；每个 `HARD` 连接的尾帧时间码、文件、连续性移交、1–2 秒尾帧识别缓冲范围、缓冲后 CUT 点和可选站位合成图；注册表与每镜资产键；资产引用自检结果；补生成资产的需求说明、最终文件、QC、平台 ID 与 token；原视频与 `(OV)` 绝对路径；旁白文本、速度、参考音频、WAV 与 cue；QC 结果及异常时间码；BGM 候选、平台 ID、理由与独立文件路径；未完成项。只有已授权阶段都有可验证文件，或真实阻塞已明确报告时才结束。

用户询问安装、准备材料、调用方法、产物命名或故障处理时，完整阅读 [manual/使用手册.md](manual/使用手册.md)。
